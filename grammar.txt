START
	| STMTS

STMTS
	| STMTS STMT
	| STMT

STMT
	| { STMTS }
	| IF ( EXPR ) STMT
	| IF ( EXPR ) STMT ELSE STMT
	| WHILE ( EXPR ) STMT
	| FOR ( VAR ID TYPE = EXPR; EXPR; EXPR ) STMT
	| CONTINUE ;
	| BREAK ;
	| VAR ID TYPE ;
	| VAR ID TYPE = EXPR ;
	| ID = EXPR ;
	| PRINT EXPR ;

EXPR
	| ( EXPR ) 
	| EXPR && EXPR
	| EXPR || EXPR
	| EXPR == EXPR
	| EXPR != EXPR
	| EXPR >= EXPR 
	| EXPR >  EXPR
	| EXPR <= EXPR
	| EXPR <  EXPR
	| EXPR *  EXPR
	| EXPR /  EXPR
	| EXPR +  EXPR
	| EXPR -  EXPR
	| ID
	| NUM

TYPE
	| INT
	| FLOAT

